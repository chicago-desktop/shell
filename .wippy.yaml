version: "1.0"

# The base is declared as its GitHub repository and resolves by tag. For
# development it is taken from the neighboring working copy instead; remove
# this replacement (and the one in test/.wippy.yaml) to build against the
# tagged base — the replacement needs that directory right next to this one.
workspace:
  replacements:
    chicago/tui-desktop: ../kickside-module
