version: "1.0"

# butschster/tui-desktop в Hub ещё не опубликован, поэтому основа берётся из
# рабочей копии по соседству. Условие снятия: как только основа опубликована,
# эту замену убрать и оставить разрешение по версии из wippy.lock — иначе
# модуль собирается только на машине, где рядом лежит нужный каталог.
workspace:
  replacements:
    butschster/tui-desktop: ../kickside-module
