-- Окно «Дата и время» — процесса внутри нет.
--
-- Запись окна обязана быть процессом (`kind: process.lua`) — так её находит
-- каталог, — но при `window_content: pixels` композитор её не запускает:
-- содержимое рисует `render` в композиторе, данные приносит `state`. Этот
-- файл существует, чтобы запись была законной, и если его всё же запустят,
-- он тихо завершится, а не притворится окном.
local function main()
    return true
end

return {main = main}
