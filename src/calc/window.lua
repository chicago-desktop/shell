-- Калькулятор — процесса внутри нет.
--
-- Запись окна обязана быть процессом, так её находит каталог; при
-- `window_content: pixels` композитор её не запускает: рисует `render`,
-- считает `state`. Запущенный вручную, файл тихо завершается.
local function main()
    return true
end

return {main = main}
